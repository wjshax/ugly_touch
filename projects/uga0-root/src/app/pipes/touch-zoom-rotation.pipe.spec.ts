/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TouchZoomRotationPipe } from './touch-zoom-rotation.pipe';

describe('Pipe: TouchZoomRotatione', () => {
  it('create an instance', () => {
    let pipe = new TouchZoomRotationPipe();
    expect(pipe).toBeTruthy();
  });
});
