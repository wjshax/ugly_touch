import { UgLibTouchGesture } from '../../ug-lib-touch-gesture.enum';
import { UgLibTouchPoint } from '../../ug-lib-touch-point';

export class GesturePreCalculator {
  public static linear(point: UgLibTouchPoint, gestures: UgLibTouchGesture[]) {
    return;
  }
}
